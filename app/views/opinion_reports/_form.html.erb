<%= form_for(opinion_report) do |f| %>
  <% if opinion_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opinion_report.errors.count, "error") %> prohibited this opinion_report from being saved:</h2>

      <ul>
      <% opinion_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :opinion_id %>
    <%= f.text_field :opinion_id %>
  </div>

  <div class="field">
    <%= f.label :reason %>
    <%= f.text_field :reason %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
