<%= form_for(@project_tag) do |f| %>
  <% if @project_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_tag.errors.count, "error") %> prohibited this project_tag from being saved:</h2>

      <ul>
      <% @project_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :law_project_id %><br>
    <%= f.text_field :law_project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
